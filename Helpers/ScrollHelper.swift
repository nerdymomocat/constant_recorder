import Foundation

class ScrollHelper {
    static func scrollTimeline() {
        // Code to scroll through the timeline
    }
    
    static func searchContent(term: String) {
        // Code to search for a term or phrase across transcripts and OCR content
    }
    
    static func openInBrowser(url: URL) {
        // Code to open a browser with the given URL
    }
}
